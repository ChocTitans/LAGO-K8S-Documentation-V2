apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/service: api
  name: api
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app.kubernetes.io/service: api
